# ASA Admin Skill

Meta-Skill runtime de **engenharia de sistemas agênticos**. Núcleo em `SKILL.md`; taxonomia e padrões em `references/`; modelos em `templates/`; casos em `tests/`.

- **Boundary:** `blueprints/ASA_ADMIN_SKILL.md` / ADR-001.
- **Pipeline:** diagnosticar → arquitetar → delegar → executar → monitorar → validar → corrigir → empacotar → evoluir.
- **Não contém:** administração pessoal de Leonardo, representação física DeskOS.

## Estrutura
```
asa-admin/
├── SKILL.md
├── README.md
├── references/   # taxonomy + component-selection
├── templates/    # blueprint-skeleton
└── tests/        # criação de Skill
```
